#include "main.h"
/**
 * add - adds two integers and returns the result
 * @m: parameter is integer
 * @n: parameter is integer
 * Return: integer always
 */
int add(int m, int n)
{
	return (m + n);
}
