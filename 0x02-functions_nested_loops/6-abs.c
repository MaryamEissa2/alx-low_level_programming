#include "main.h"
/**
 * _abs -  computes the absolute value of an integer
 * @r: pramater is integer
 * Return: integer always
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}
}
